 pre-commit:
   parallel: true
   commands:
     gofmt:
       glob: "*.go"
       run: gofmt -w {staged_files}
     govet:
       files: git ls-files -m
       glob: "*.go"
       run: go vet {files}
